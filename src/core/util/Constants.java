package core.util;

public class Constants {
    public static int WIDTH = 1200;
    public static int HEIGHT = 720;
    public static int numberOfBars = 300; // 400 is limit such that there isn't blank space at the end
    //1200 also works well
    //600 works well too
    // potential cause is rounding errors
}
